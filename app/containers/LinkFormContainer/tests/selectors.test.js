// import { selectLinkFormContainerDomain } from '../selectors';

describe('selectLinkFormContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
